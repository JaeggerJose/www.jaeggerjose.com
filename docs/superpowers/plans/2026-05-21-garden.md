# Garden Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 www.jaeggerjose.com 新增 `/garden/`：are.na 風格的 channel × block 收藏系統，混合連結、圖片、短筆記三型，公開可看、admin 可建。

**Architecture:** 純靜態 HTML/CSS/JS + Supabase（auth / table / storage / RLS）+ 一支 Vercel Function (`/api/og`) 抓 og 自動預填。`vercel.json` rewrite 把 `/garden/[slug]/` 映到單一 channel template。沒有引入 framework / SSG。

**Tech Stack:** vanilla HTML/CSS/JS、Supabase JS SDK v2、Vercel Functions (Node.js)、Press Start 2P + Shippori Mincho + Noto Sans。

**Reference spec:** [docs/superpowers/specs/2026-05-21-garden-design.md](../specs/2026-05-21-garden-design.md)

**XSS 防護模式（全 JS 共用）：** 所有 DOM 內容透過 `escapeHtml()` 套字串跳脫，再用 `setHTML(el, str)` helper（內部用 `Range.createContextualFragment`）寫入。等價於 escaped `.innerHTML` setter，但走 Range API 比較現代。

```js
// 每個 js 檔案頂端都放這兩個 helper
function escapeHtml(s) {
  return String(s ?? '')
    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
}
function setHTML(el, html) {
  el.replaceChildren(document.createRange().createContextualFragment(html));
}
```

---

## Phase 0 — Database & Storage

### Task 0.1: 建立 garden_channels / garden_blocks 表 + RLS

**Files:**
- Create: `supabase/migrations/20260521120000_garden.sql`

- [ ] **Step 1: 寫 migration SQL 並儲存到本機**

```sql
-- supabase/migrations/20260521120000_garden.sql

create table garden_channels (
  id          uuid primary key default gen_random_uuid(),
  slug        text unique not null,
  title       text not null,
  summary     text,
  visibility  text not null default 'public'
                check (visibility in ('public','private')),
  position    int not null default 0,
  created_at  timestamptz default now(),
  updated_at  timestamptz default now()
);

create index garden_channels_visibility_position
  on garden_channels (visibility, position);

create table garden_blocks (
  id           uuid primary key default gen_random_uuid(),
  channel_id   uuid not null
                references garden_channels(id) on delete cascade,
  type         text not null
                check (type in ('link','image','note')),
  title        text,
  body         text,
  url          text,
  image_path   text,
  og_image     text,
  tags         text[] not null default '{}',
  position     int not null default 0,
  created_at   timestamptz default now()
);

create index garden_blocks_channel_position
  on garden_blocks (channel_id, position desc, created_at desc);

alter table garden_channels enable row level security;
alter table garden_blocks   enable row level security;

create policy garden_channels_anon_read
  on garden_channels for select to anon
  using (visibility = 'public');

create policy garden_blocks_anon_read
  on garden_blocks for select to anon
  using (
    exists (
      select 1 from garden_channels c
      where c.id = garden_blocks.channel_id
        and c.visibility = 'public'
    )
  );

create policy garden_channels_auth_all
  on garden_channels for all to authenticated
  using (true) with check (true);

create policy garden_blocks_auth_all
  on garden_blocks for all to authenticated
  using (true) with check (true);
```

- [ ] **Step 2: 透過 Supabase MCP 套用 migration**

呼叫 `mcp__plugin_supabase_supabase__apply_migration`：
- `name`: `garden_schema`
- `query`: 上述完整 SQL

預期：success: true。

- [ ] **Step 3: 確認 schema**

`mcp__plugin_supabase_supabase__list_tables` schemas=['public']。
預期清單含 `garden_channels` 與 `garden_blocks`。

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260521120000_garden.sql
git commit -m "feat(garden): add channels/blocks schema + RLS"
```

---

### Task 0.2: 建立 garden-images storage bucket

- [ ] **Step 1: 建 bucket（呼叫 mcp execute_sql）**

```sql
insert into storage.buckets (id, name, public)
values ('garden-images', 'garden-images', true)
on conflict (id) do nothing;
```

預期：affected 0 或 1 row。

- [ ] **Step 2: 加 storage policies**

```sql
create policy "garden-images public read"
  on storage.objects for select to anon
  using (bucket_id = 'garden-images');

create policy "garden-images auth write"
  on storage.objects for insert to authenticated
  with check (bucket_id = 'garden-images');

create policy "garden-images auth update"
  on storage.objects for update to authenticated
  using (bucket_id = 'garden-images');

create policy "garden-images auth delete"
  on storage.objects for delete to authenticated
  using (bucket_id = 'garden-images');
```

- [ ] **Step 3: 確認 bucket**

```sql
select id, name, public from storage.buckets where id = 'garden-images';
```

預期：1 row, public=true。

---

### Task 0.3: RLS 冒煙驗證

- [ ] **Step 1: 插測試資料**

```sql
insert into garden_channels (slug, title, summary, visibility, position)
values ('test-public', 'Test Public', 'rls public probe', 'public', 0),
       ('test-private', 'Test Private', 'rls private probe', 'private', 1);
```

- [ ] **Step 2: 用 anon key fetch（curl）**

```bash
ANON='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Impud25pZ3d5eWFzZHRzZ2xpeXBhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzY4MjkzOTMsImV4cCI6MjA5MjQwNTM5M30.62cJEKNqTt6P6bZMrVVcp_xLbD6tIuTjKgOI-SmSMAY'
curl -s "https://jnwnigwyyasdtsgliypa.supabase.co/rest/v1/garden_channels?select=slug,visibility" \
  -H "apikey: $ANON" -H "Authorization: Bearer $ANON"
```

預期：只回 `[{"slug":"test-public","visibility":"public"}]`，看不到 private。

- [ ] **Step 3: 清理**

```sql
delete from garden_channels where slug in ('test-public','test-private');
```

---

## Phase 1 — /api/og endpoint

### Task 1.1: SSRF 防護工具函式

**Files:**
- Create: `api/og.js`

- [ ] **Step 1: 寫常數與 helpers**

```js
// api/og.js
'use strict';

const FETCH_TIMEOUT_MS = 5000;
const MAX_BYTES = 1_000_000;
const MAX_REDIRECTS = 3;
const UA = 'Mozilla/5.0 (compatible; jaeggerjose-og-fetch/1.0; +https://www.jaeggerjose.com)';

const PRIVATE_CIDRS = [
  /^10\./, /^127\./, /^169\.254\./,
  /^172\.(1[6-9]|2\d|3[01])\./,
  /^192\.168\./, /^192\.0\.0\./, /^0\./,
];

function isPrivateIp(addr) {
  if (!addr) return true;
  if (PRIVATE_CIDRS.some(re => re.test(addr))) return true;
  if (addr === '::1' || addr === '::') return true;
  if (/^fe80:/i.test(addr) || /^fc[0-9a-f]{2}:/i.test(addr) || /^fd[0-9a-f]{2}:/i.test(addr)) return true;
  if (/^::ffff:/i.test(addr)) {
    const v4 = addr.replace(/^::ffff:/i, '');
    return PRIVATE_CIDRS.some(re => re.test(v4));
  }
  return false;
}

async function isPrivateHost(hostname) {
  const dns = require('dns').promises;
  if (/^localhost$|\.localhost$|\.local$|\.internal$/i.test(hostname)) return true;
  try {
    const addrs = await dns.lookup(hostname, { all: true });
    return addrs.some(a => isPrivateIp(a.address));
  } catch {
    return true;
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add api/og.js
git commit -m "feat(og): scaffold SSRF guards"
```

---

### Task 1.2: 帶 timeout / size cap 的 fetch

**Files:**
- Modify: `api/og.js`（append）

- [ ] **Step 1: 追加 fetchWithLimits**

```js
async function fetchWithLimits(url, redirectsLeft = MAX_REDIRECTS) {
  const u = new URL(url);
  if (u.protocol !== 'https:') throw new Error('blocked_protocol');
  if (await isPrivateHost(u.hostname)) throw new Error('blocked_host');

  const ctrl = new AbortController();
  const timer = setTimeout(() => ctrl.abort(), FETCH_TIMEOUT_MS);

  let res;
  try {
    res = await fetch(url, {
      signal: ctrl.signal,
      redirect: 'manual',
      headers: { 'User-Agent': UA, 'Accept': 'text/html,application/xhtml+xml' },
    });
  } finally {
    clearTimeout(timer);
  }

  if (res.status >= 300 && res.status < 400) {
    if (redirectsLeft <= 0) throw new Error('too_many_redirects');
    const loc = res.headers.get('location');
    if (!loc) throw new Error('redirect_no_location');
    return fetchWithLimits(new URL(loc, url).toString(), redirectsLeft - 1);
  }
  if (!res.ok) throw new Error('fetch_failed_' + res.status);

  const reader = res.body.getReader();
  const chunks = [];
  let received = 0;
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    received += value.length;
    if (received > MAX_BYTES) { try { reader.cancel(); } catch {} break; }
    chunks.push(value);
  }
  const buf = Buffer.concat(chunks.map(c => Buffer.from(c)));
  return { url, html: buf.toString('utf-8', 0, Math.min(buf.length, MAX_BYTES)) };
}
```

- [ ] **Step 2: Commit**

```bash
git add api/og.js
git commit -m "feat(og): bounded fetch with redirect re-check"
```

---

### Task 1.3: og meta 解析

**Files:**
- Modify: `api/og.js`（append）

- [ ] **Step 1: parseMeta**

```js
function attr(html, tagRe) {
  const m = html.match(tagRe);
  return m ? m[1].trim() : null;
}

function decodeHtml(s) {
  if (!s) return s;
  return s.replace(/&amp;/g, '&').replace(/&lt;/g, '<').replace(/&gt;/g, '>')
          .replace(/&quot;/g, '"').replace(/&#39;/g, "'").replace(/&apos;/g, "'");
}

function parseMeta(html, finalUrl) {
  const get = (prop) => attr(html, new RegExp(
    '<meta[^>]+(?:property|name)=["\']' + prop + '["\'][^>]+content=["\']([^"\']+)["\']', 'i'
  )) || attr(html, new RegExp(
    '<meta[^>]+content=["\']([^"\']+)["\'][^>]+(?:property|name)=["\']' + prop + '["\']', 'i'
  ));

  const titleTag = attr(html, /<title[^>]*>([\s\S]*?)<\/title>/i);
  const canonical = attr(html, /<link[^>]+rel=["\']canonical["\'][^>]+href=["\']([^"\']+)["\']/i);
  const iconHref  = attr(html, /<link[^>]+rel=["\'](?:shortcut )?icon["\'][^>]+href=["\']([^"\']+)["\']/i);

  const abs = (u) => { try { return new URL(u, finalUrl).toString(); } catch { return null; } };

  return {
    title:       decodeHtml(get('og:title') || titleTag),
    description: decodeHtml(get('og:description') || get('description')),
    image:       abs(get('og:image') || iconHref),
    site_name:   decodeHtml(get('og:site_name') || new URL(finalUrl).host),
    canonical:   abs(canonical) || finalUrl,
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add api/og.js
git commit -m "feat(og): meta parser with og/twitter/fallback"
```

---

### Task 1.4: handler + caching

**Files:**
- Modify: `api/og.js`（append）

- [ ] **Step 1: 加 handler**

```js
module.exports = async function handler(req, res) {
  const url = req.query && req.query.url;
  if (!url) return res.status(400).json({ ok: false, error: 'missing_url' });

  let target;
  try { target = new URL(url); }
  catch { return res.status(400).json({ ok: false, error: 'invalid_url' }); }
  if (target.protocol !== 'https:') {
    return res.status(400).json({ ok: false, error: 'blocked_protocol' });
  }

  try {
    if (await isPrivateHost(target.hostname)) {
      return res.status(400).json({ ok: false, error: 'blocked_host' });
    }
    const fetched = await fetchWithLimits(target.toString());
    const meta = parseMeta(fetched.html, fetched.url);
    res.setHeader('Cache-Control', 's-maxage=86400, stale-while-revalidate=604800');
    return res.json({ ok: true, ...meta });
  } catch (err) {
    return res.status(400).json({ ok: false, error: String(err.message || err) });
  }
};
```

- [ ] **Step 2: Commit + push**

```bash
git add api/og.js
git commit -m "feat(og): HTTP handler with SSRF + CDN cache"
git push origin main
```

---

### Task 1.5: 部署驗證

- [ ] **Step 1: 等部署 ~30s**

- [ ] **Step 2: 合法 URL**

```bash
curl -s "https://www.jaeggerjose.com/api/og?url=https%3A%2F%2Fexample.com" | head -c 400
```

預期：`{"ok":true,"title":"Example Domain",...}`。

- [ ] **Step 3: SSRF http 阻擋**

```bash
curl -s "https://www.jaeggerjose.com/api/og?url=http%3A%2F%2F169.254.169.254%2F"
```

預期：`{"ok":false,"error":"blocked_protocol"}`。

- [ ] **Step 4: SSRF private IP 阻擋**

```bash
curl -s "https://www.jaeggerjose.com/api/og?url=https%3A%2F%2F127.0.0.1%2F"
```

預期：`{"ok":false,"error":"blocked_host"}`。

---

## Phase 2 — Landing /garden/

### Task 2.1: garden.html

**Files:**
- Create: `garden.html`

- [ ] **Step 1: 寫 HTML（與 journal.html 同模式）**

```html
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>GARDEN — Jaegger Jose</title>
  <meta name="description" content="A place where I keep links, images, and notes." />

  <link rel="icon" href="favicon.svg" />
  <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=Shippori+Mincho+B1:wght@400;600;700&family=Noto+Sans:ital,wght@0,300;0,400;1,300&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/style.css?v=9">
  <link rel="stylesheet" href="css/garden.css?v=1">

  <script async src="https://www.googletagmanager.com/gtag/js?id=G-H8SCHM4MZK"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-H8SCHM4MZK');
  </script>
</head>
<body class="page-garden">

  <header class="site-header">
    <a href="/" class="brand">JaeggerJose</a>
    <nav class="site-nav">
      <ul>
        <li><a href="/#about">ABOUT</a></li>
        <li><a href="journal.html">LIFE</a></li>
        <li><a href="works.html">PROJECTS</a></li>
        <li><a href="garden.html" aria-current="page">GARDEN</a></li>
        <li><a href="/#contact">CONTACT</a></li>
      </ul>
    </nav>
  </header>

  <main class="garden-main">
    <section class="garden-hero">
      <h1 class="garden-title">GARDEN</h1>
      <p class="garden-subtitle">a place where i keep things</p>
    </section>
    <section class="garden-channels" id="channels-list" aria-busy="true">
      <p class="garden-empty">loading…</p>
    </section>
  </main>

  <div class="auth-bar" id="auth-bar" style="display:none;">
    <span id="auth-user"></span>
    <button id="new-channel-btn">+ NEW CHANNEL</button>
    <button id="signout-btn">SIGN OUT</button>
  </div>
  <button class="auth-trigger" id="auth-trigger" aria-label="admin login">·</button>

  <dialog id="channel-modal" class="garden-modal">
    <form method="dialog" id="channel-form">
      <h2>New Channel</h2>
      <label>Title <input name="title" required></label>
      <label>Slug  <input name="slug" required pattern="[a-z0-9-]+"></label>
      <label>Summary <textarea name="summary" rows="3"></textarea></label>
      <label>Visibility
        <select name="visibility">
          <option value="public" selected>Public</option>
          <option value="private">Private</option>
        </select>
      </label>
      <label>Position <input name="position" type="number" value="0"></label>
      <div class="garden-modal-actions">
        <button value="cancel" type="reset">Cancel</button>
        <button value="confirm" type="submit">Create</button>
      </div>
    </form>
  </dialog>

  <dialog id="login-modal" class="garden-modal">
    <form method="dialog" id="login-form">
      <h2>Admin Login</h2>
      <label>Email <input name="email" type="email" required></label>
      <label>Password <input name="password" type="password" required></label>
      <div class="garden-modal-actions">
        <button value="cancel" type="reset">Cancel</button>
        <button value="confirm" type="submit">Sign in</button>
      </div>
    </form>
  </dialog>

  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.min.js"></script>
  <script src="js/main.js?v=5"></script>
  <script src="js/garden.js?v=1"></script>
  <script defer src="/_vercel/insights/script.js"></script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add garden.html
git commit -m "feat(garden): /garden/ landing HTML"
```

---

### Task 2.2: css/garden.css

**Files:**
- Create: `css/garden.css`

- [ ] **Step 1: 全部 CSS（landing + 後續 channel 頁共用 modal）**

```css
/* css/garden.css */

.garden-main {
  max-width: 1080px;
  margin: 0 auto;
  padding: clamp(2rem, 4vw, 5rem) clamp(1rem, 3vw, 3rem);
}

.garden-hero { margin-bottom: clamp(2rem, 4vw, 4rem); }
.garden-title {
  font-family: var(--font-pixel);
  font-size: clamp(2rem, 5vw, 4rem);
  letter-spacing: 0.02em;
  color: var(--ink);
  margin: 0 0 0.5rem;
}
.garden-subtitle {
  font-family: 'Noto Sans', sans-serif;
  font-size: 1rem;
  color: var(--ink-dim);
  margin: 0;
}

.garden-channels { border-top: 1px solid var(--muted-border); }

.garden-channel-row {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 1rem;
  padding: 1.2rem 0;
  border-bottom: 1px solid var(--muted-border);
  text-decoration: none;
  color: inherit;
  position: relative;
  isolation: isolate;
}
.garden-channel-row::after {
  content: '';
  position: absolute;
  inset: 0;
  background-image: radial-gradient(circle, var(--ink) 1px, transparent 1px);
  background-size: 4px 4px;
  opacity: 0;
  transition: opacity 250ms cubic-bezier(0.16, 1, 0.3, 1);
  pointer-events: none;
  z-index: -1;
  mix-blend-mode: multiply;
}
.garden-channel-row:hover::after,
.garden-channel-row:focus-visible::after { opacity: 0.18; }

.garden-channel-row .row-title {
  font-family: var(--font-pixel);
  font-size: 0.95rem;
  letter-spacing: 0.05em;
  text-transform: uppercase;
}
.garden-channel-row .row-summary {
  font-family: 'Noto Sans', sans-serif;
  font-size: 0.85rem;
  color: var(--ink-dim);
  margin-top: 0.4rem;
}
.garden-channel-row .row-count {
  font-family: var(--font-pixel);
  font-size: 0.75rem;
  color: var(--ink-dim);
  align-self: center;
}
.garden-channel-row .row-private-badge {
  display: inline-block;
  font-family: var(--font-pixel);
  font-size: 0.6rem;
  padding: 2px 6px;
  background: var(--akache-dim);
  color: var(--akache);
  margin-left: 0.5rem;
}

.garden-empty {
  font-family: 'Noto Sans', sans-serif;
  color: var(--ink-dim);
  text-align: center;
  padding: 3rem 0;
}

.garden-modal {
  border: 1px solid var(--ink);
  background: var(--paper);
  color: var(--ink);
  padding: 1.5rem;
  max-width: 480px;
  width: calc(100% - 2rem);
  font-family: 'Noto Sans', sans-serif;
}
.garden-modal::backdrop { background: rgba(26, 20, 16, 0.55); }
.garden-modal h2 {
  font-family: var(--font-pixel);
  font-size: 1rem;
  margin: 0 0 1rem;
}
.garden-modal label { display: block; margin-bottom: 0.8rem; font-size: 0.85rem; }
.garden-modal input,
.garden-modal textarea,
.garden-modal select {
  width: 100%;
  padding: 0.5rem;
  border: 1px solid var(--muted-border);
  background: var(--paper-alt);
  font-family: inherit;
  font-size: 0.9rem;
  margin-top: 0.3rem;
}
.garden-modal-actions {
  display: flex;
  gap: 0.8rem;
  justify-content: flex-end;
  margin-top: 1rem;
}
.garden-modal-actions button {
  font-family: var(--font-pixel);
  font-size: 0.7rem;
  padding: 0.6rem 1rem;
  border: 1px solid var(--ink);
  background: var(--paper-alt);
  cursor: pointer;
}
.garden-modal-actions button[value="confirm"] {
  background: var(--moss);
  color: var(--paper);
  border-color: var(--moss);
}

.auth-bar {
  position: fixed; bottom: 1rem; right: 1rem;
  background: var(--paper);
  border: 1px solid var(--ink);
  padding: 0.6rem 0.8rem;
  display: flex;
  gap: 0.6rem;
  font-family: var(--font-pixel);
  font-size: 0.65rem;
  z-index: 50;
}
.auth-bar button {
  font: inherit; background: var(--paper-alt);
  border: 1px solid var(--ink); padding: 0.3rem 0.6rem;
  cursor: pointer;
}
.auth-trigger {
  position: fixed; bottom: 0.5rem; right: 0.5rem;
  width: 12px; height: 12px;
  border-radius: 50%;
  background: var(--muted-border);
  border: none;
  cursor: pointer;
  opacity: 0.3;
  z-index: 49;
}
.auth-trigger:hover { opacity: 1; }

body:not(.is-admin) [data-admin] { display: none !important; }

@media (max-width: 600px) {
  .garden-channel-row { grid-template-columns: 1fr; }
  .garden-channel-row .row-count { justify-self: start; }
}

/* ===== Channel page ===== */
.channel-breadcrumb {
  font-family: var(--font-pixel);
  font-size: 0.7rem;
  margin-bottom: 1rem;
  color: var(--ink-dim);
}
.channel-breadcrumb a { color: inherit; text-decoration: none; }
.channel-breadcrumb a:hover { color: var(--moss); }

.garden-channel-hero { margin-bottom: 2rem; }

.garden-block-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap: 1rem;
  margin-bottom: 3rem;
}

.garden-block-card {
  aspect-ratio: 1 / 1;
  border: 1px solid var(--ink);
  background: var(--paper-alt);
  position: relative;
  overflow: hidden;
  cursor: pointer;
  contain: paint;
  display: flex;
  flex-direction: column;
}
.garden-block-card .badge {
  position: absolute;
  top: 6px; left: 6px;
  font-family: var(--font-pixel);
  font-size: 0.55rem;
  padding: 3px 6px;
  z-index: 2;
}
.garden-block-card[data-type="link"]  .badge { background: var(--moss);   color: var(--paper); }
.garden-block-card[data-type="image"] .badge { background: var(--akache); color: var(--paper); }
.garden-block-card[data-type="note"]  .badge { background: var(--ink);    color: var(--paper); }

.garden-block-card .block-thumb {
  flex: 1;
  width: 100%;
  object-fit: cover;
  background: var(--paper-deep);
  display: block;
}
.garden-block-card .block-note-preview {
  flex: 1;
  padding: 1.5rem 1rem 1rem;
  font-family: 'Shippori Mincho B1', serif;
  font-size: 0.95rem;
  line-height: 1.5;
  color: var(--ink);
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 6;
  -webkit-box-orient: vertical;
}
.garden-block-card .block-foot {
  padding: 0.5rem 0.6rem;
  font-family: var(--font-pixel);
  font-size: 0.55rem;
  border-top: 1px solid var(--muted-border);
  color: var(--ink-dim);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.garden-block-card .block-actions {
  position: absolute;
  top: 6px; right: 6px;
  display: none;
  gap: 4px;
  z-index: 2;
}
.garden-block-card:hover .block-actions { display: flex; }
.garden-block-card .block-actions button {
  font-family: var(--font-pixel);
  font-size: 0.55rem;
  padding: 3px 6px;
  background: var(--paper);
  border: 1px solid var(--ink);
  cursor: pointer;
}

.garden-block-modal {
  max-width: 720px;
  padding: 0;
}
.garden-block-modal .modal-close {
  position: absolute; top: 8px; right: 12px;
  background: none; border: none;
  font-size: 1.5rem; cursor: pointer;
  color: var(--ink); z-index: 3;
}
.garden-block-modal .modal-hero {
  width: 100%;
  max-height: 60vh;
  object-fit: contain;
  background: var(--paper-deep);
  display: block;
}
.garden-block-modal .modal-quote {
  font-family: 'Shippori Mincho B1', serif;
  font-size: 1.4rem;
  line-height: 1.6;
  padding: 3rem 2rem 1rem;
}
.garden-block-modal .modal-body { padding: 1.5rem 2rem; }
.garden-block-modal .modal-title {
  font-family: 'Shippori Mincho B1', serif;
  font-size: 1.4rem;
  font-weight: 700;
  margin: 0 0 0.6rem;
}
.garden-block-modal .modal-comment {
  font-family: 'Noto Sans', sans-serif;
  font-size: 1rem;
  color: var(--ink-dim);
  margin: 0 0 1rem;
}
.garden-block-modal .modal-tags { margin-bottom: 1rem; }
.garden-block-modal .modal-tags span {
  display: inline-block;
  font-family: var(--font-pixel);
  font-size: 0.6rem;
  padding: 3px 7px;
  background: var(--paper-alt);
  border: 1px solid var(--muted-border);
  margin-right: 4px;
}
.garden-block-modal .modal-meta {
  font-family: var(--font-pixel);
  font-size: 0.6rem;
  color: var(--ink-dim);
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-top: 1px solid var(--muted-border);
  padding-top: 0.8rem;
}
.garden-block-modal .modal-visit {
  font-family: var(--font-pixel);
  font-size: 0.7rem;
  padding: 0.5rem 0.9rem;
  background: var(--moss);
  color: var(--paper);
  text-decoration: none;
  display: inline-block;
}

fieldset {
  border: 1px solid var(--muted-border);
  padding: 0.6rem;
  margin-bottom: 0.8rem;
  font-size: 0.85rem;
}
fieldset legend { font-family: var(--font-pixel); font-size: 0.65rem; padding: 0 0.4rem; }
fieldset label { display: inline-block; margin-right: 1rem; margin-bottom: 0; }
```

- [ ] **Step 2: Commit**

```bash
git add css/garden.css
git commit -m "feat(garden): full CSS for landing + channel + modal"
```

---

### Task 2.3: js/garden.js — channels list + admin

**Files:**
- Create: `js/garden.js`

- [ ] **Step 1: 寫完整 landing JS**

```js
// js/garden.js
'use strict';

const SUPABASE_URL  = 'https://jnwnigwyyasdtsgliypa.supabase.co';
const SUPABASE_ANON = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Impud25pZ3d5eWFzZHRzZ2xpeXBhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzY4MjkzOTMsImV4cCI6MjA5MjQwNTM5M30.62cJEKNqTt6P6bZMrVVcp_xLbD6tIuTjKgOI-SmSMAY';
const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON);

function escapeHtml(s) {
  return String(s ?? '')
    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
}
function setHTML(el, html) {
  el.replaceChildren(document.createRange().createContextualFragment(html));
}

const listEl = document.getElementById('channels-list');
let isAdmin = false;

async function loadChannels() {
  const { data, error } = await sb
    .from('garden_channels')
    .select('id, slug, title, summary, visibility, position')
    .order('position', { ascending: true })
    .order('created_at', { ascending: false });

  if (error) {
    setHTML(listEl, `<p class="garden-empty">failed to load (${escapeHtml(error.message)})</p>`);
    listEl.setAttribute('aria-busy', 'false');
    return;
  }
  if (!data || data.length === 0) {
    setHTML(listEl, `<p class="garden-empty">no channels yet.</p>`);
    listEl.setAttribute('aria-busy', 'false');
    return;
  }

  const channelIds = data.map(c => c.id);
  const { data: counts } = await sb
    .from('garden_blocks')
    .select('channel_id')
    .in('channel_id', channelIds);
  const countMap = {};
  for (const row of (counts || [])) {
    countMap[row.channel_id] = (countMap[row.channel_id] || 0) + 1;
  }

  const rows = data.map(c => {
    const isPriv = c.visibility === 'private';
    const cnt = countMap[c.id] || 0;
    return `<a class="garden-channel-row" href="/garden/${encodeURIComponent(c.slug)}/" data-id="${c.id}">
      <div>
        <div class="row-title">${escapeHtml(c.title)}${isPriv ? '<span class="row-private-badge">PRIV</span>' : ''}</div>
        ${c.summary ? `<div class="row-summary">${escapeHtml(c.summary)}</div>` : ''}
      </div>
      <div class="row-count">(${cnt})</div>
    </a>`;
  }).join('');
  setHTML(listEl, rows);
  listEl.setAttribute('aria-busy', 'false');
}

// Auth
const authBar     = document.getElementById('auth-bar');
const authTrigger = document.getElementById('auth-trigger');
const loginModal  = document.getElementById('login-modal');
const loginForm   = document.getElementById('login-form');
const signoutBtn  = document.getElementById('signout-btn');
const authUser    = document.getElementById('auth-user');

authTrigger.addEventListener('click', () => {
  if (isAdmin) authBar.style.display = 'flex';
  else loginModal.showModal();
});
loginForm.addEventListener('submit', async (e) => {
  const fd = new FormData(loginForm);
  const { error } = await sb.auth.signInWithPassword({
    email: fd.get('email'), password: fd.get('password'),
  });
  if (error) { alert('Login failed: ' + error.message); e.preventDefault(); }
});
signoutBtn.addEventListener('click', async () => {
  await sb.auth.signOut();
  authBar.style.display = 'none';
});
sb.auth.onAuthStateChange((_evt, session) => {
  isAdmin = !!session;
  document.body.classList.toggle('is-admin', !!session);
  if (session) { authBar.style.display = 'flex'; authUser.textContent = session.user.email; }
  else authBar.style.display = 'none';
  loadChannels();
});

// New channel
const newBtn       = document.getElementById('new-channel-btn');
const channelModal = document.getElementById('channel-modal');
const channelForm  = document.getElementById('channel-form');

newBtn.addEventListener('click', () => channelModal.showModal());

channelForm.addEventListener('submit', async (e) => {
  if (e.submitter && e.submitter.value === 'cancel') return;
  const fd = new FormData(channelForm);
  const payload = {
    title:      fd.get('title'),
    slug:       String(fd.get('slug') || '').toLowerCase().replace(/[^a-z0-9-]/g, '-'),
    summary:    fd.get('summary') || null,
    visibility: fd.get('visibility'),
    position:   Number(fd.get('position') || 0),
  };
  const { error } = await sb.from('garden_channels').insert(payload);
  if (error) { alert('Create failed: ' + error.message); e.preventDefault(); return; }
  channelForm.reset();
  loadChannels();
});

channelForm.title.addEventListener('input', () => {
  if (channelForm.slug.dataset.touched !== 'true') {
    channelForm.slug.value = channelForm.title.value
      .toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
  }
});
channelForm.slug.addEventListener('input', () => {
  channelForm.slug.dataset.touched = 'true';
});

loadChannels();
```

- [ ] **Step 2: Commit + push**

```bash
git add js/garden.js
git commit -m "feat(garden): landing page logic (channels + admin)"
git push origin main
```

---

### Task 2.4: 部署冒煙測

- [ ] **Step 1: 等部署 ~30s，Playwright 訪問**

```
mcp__plugin_playwright_playwright__browser_navigate
  url: https://www.jaeggerjose.com/garden.html
```

- [ ] **Step 2: 截圖**

```
mcp__plugin_playwright_playwright__browser_take_screenshot
  filename: garden-landing-empty.png
  fullPage: true
```

預期：看到 GARDEN hero + `no channels yet.`。

- [ ] **Step 3: 手動登入 + 建一個 channel**

操作（透過 Playwright click 或瀏覽器手動）：
1. 點右下小圓點 `.auth-trigger`
2. 登入
3. 點 `+ NEW CHANNEL`
4. 填 title=`Typography`, summary=`notes on typesetting i can't unsee`，submit
5. 重新整理，確認列表出現 TYPOGRAPHY (0)

---

## Phase 3 — Channel page /garden/[slug]/

### Task 3.1: Vercel rewrite

**Files:**
- Modify: `vercel.json`

- [ ] **Step 1: 加 rewrites 區塊**

讀 vercel.json，在 `headers` 陣列之後（同層）加：

```json
  "rewrites": [
    { "source": "/garden/:slug", "destination": "/garden/_channel.html" },
    { "source": "/garden/:slug/", "destination": "/garden/_channel.html" }
  ]
```

注意：要在 `headers` 之後加逗號，rewrites 加到最後。確認 JSON 仍合法。

- [ ] **Step 2: Commit**

```bash
git add vercel.json
git commit -m "feat(garden): rewrite /garden/:slug to channel template"
```

---

### Task 3.2: garden/_channel.html

**Files:**
- Create: `garden/_channel.html`

- [ ] **Step 1: 寫 channel HTML（注意所有 asset 用絕對路徑 /…，因為 URL 變深）**

```html
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Channel — Garden — Jaegger Jose</title>
  <link rel="icon" href="/favicon.svg" />
  <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=Shippori+Mincho+B1:wght@400;600;700&family=Noto+Sans:ital,wght@0,300;0,400;1,300&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css?v=9">
  <link rel="stylesheet" href="/css/garden.css?v=1">

  <script async src="https://www.googletagmanager.com/gtag/js?id=G-H8SCHM4MZK"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-H8SCHM4MZK');
  </script>
</head>
<body class="page-garden">

  <header class="site-header">
    <a href="/" class="brand">JaeggerJose</a>
    <nav class="site-nav">
      <ul>
        <li><a href="/#about">ABOUT</a></li>
        <li><a href="/journal.html">LIFE</a></li>
        <li><a href="/works.html">PROJECTS</a></li>
        <li><a href="/garden.html" aria-current="page">GARDEN</a></li>
        <li><a href="/#contact">CONTACT</a></li>
      </ul>
    </nav>
  </header>

  <main class="garden-main">
    <nav class="channel-breadcrumb">
      <a href="/garden/">← GARDEN</a>
      <span> / </span>
      <span id="channel-name">…</span>
    </nav>

    <section class="garden-hero garden-channel-hero">
      <h1 class="garden-title" id="channel-title">…</h1>
      <p class="garden-subtitle" id="channel-summary"></p>
    </section>

    <section class="garden-block-grid" id="block-grid" aria-busy="true">
      <p class="garden-empty">loading…</p>
    </section>

    <section class="garden-channel-tags" id="channel-tags" hidden>
      <h2>Tags in this channel</h2>
      <div id="tag-cloud"></div>
    </section>
  </main>

  <dialog id="block-modal" class="garden-modal garden-block-modal">
    <button class="modal-close" aria-label="close">×</button>
    <div id="block-modal-body"></div>
  </dialog>

  <dialog id="block-form-modal" class="garden-modal">
    <form method="dialog" id="block-form">
      <h2 id="block-form-title">New Block</h2>
      <fieldset>
        <legend>Type</legend>
        <label><input type="radio" name="type" value="link" checked> Link</label>
        <label><input type="radio" name="type" value="image"> Image</label>
        <label><input type="radio" name="type" value="note"> Note</label>
      </fieldset>

      <div data-type-field="link">
        <label>URL
          <div style="display:flex; gap:0.4rem;">
            <input name="url" type="url" placeholder="https://...">
            <button type="button" id="og-fetch-btn">→ Fetch</button>
          </div>
        </label>
        <label>OG Image URL
          <input name="og_image" type="url" placeholder="auto-filled">
        </label>
      </div>

      <div data-type-field="image">
        <label>Image File
          <input name="image_file" type="file" accept="image/*">
        </label>
      </div>

      <label>Title <input name="title"></label>
      <label>Body / Comment <textarea name="body" rows="3"></textarea></label>
      <label>Tags (comma-separated)
        <input name="tags" placeholder="typography, print">
      </label>
      <label>Position <input name="position" type="number" value="0"></label>

      <div class="garden-modal-actions">
        <button value="cancel" type="reset">Cancel</button>
        <button value="confirm" type="submit">Publish</button>
      </div>
    </form>
  </dialog>

  <div class="auth-bar" id="auth-bar" style="display:none;">
    <span id="auth-user"></span>
    <button id="new-block-btn">+ NEW BLOCK</button>
    <button id="signout-btn">SIGN OUT</button>
  </div>
  <button class="auth-trigger" id="auth-trigger" aria-label="admin login">·</button>

  <dialog id="login-modal" class="garden-modal">
    <form method="dialog" id="login-form">
      <h2>Admin Login</h2>
      <label>Email <input name="email" type="email" required></label>
      <label>Password <input name="password" type="password" required></label>
      <div class="garden-modal-actions">
        <button value="cancel" type="reset">Cancel</button>
        <button value="confirm" type="submit">Sign in</button>
      </div>
    </form>
  </dialog>

  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.min.js"></script>
  <script src="/js/main.js?v=5"></script>
  <script src="/js/garden-channel.js?v=1"></script>
  <script defer src="/_vercel/insights/script.js"></script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
mkdir -p garden
git add garden/_channel.html
git commit -m "feat(garden): channel page template"
```

---

### Task 3.3: js/garden-channel.js — Part 1（init + render）

**Files:**
- Create: `js/garden-channel.js`

- [ ] **Step 1: 寫初始化與 render**

```js
// js/garden-channel.js
'use strict';

const SUPABASE_URL  = 'https://jnwnigwyyasdtsgliypa.supabase.co';
const SUPABASE_ANON = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Impud25pZ3d5eWFzZHRzZ2xpeXBhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzY4MjkzOTMsImV4cCI6MjA5MjQwNTM5M30.62cJEKNqTt6P6bZMrVVcp_xLbD6tIuTjKgOI-SmSMAY';
const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON);

function escapeHtml(s) {
  return String(s ?? '')
    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
}
function setHTML(el, html) {
  el.replaceChildren(document.createRange().createContextualFragment(html));
}

const slug = decodeURIComponent(
  location.pathname.replace(/^\/garden\//, '').replace(/\/$/, '')
);

let currentChannel = null;
let currentBlocks  = [];
let isAdmin = false;

const gridEl    = document.getElementById('block-grid');
const titleEl   = document.getElementById('channel-title');
const summaryEl = document.getElementById('channel-summary');
const nameEl    = document.getElementById('channel-name');
const tagsBox   = document.getElementById('channel-tags');
const tagCloud  = document.getElementById('tag-cloud');

function renderBlockCard(b) {
  let inner;
  if (b.type === 'link') {
    inner = b.og_image
      ? `<img class="block-thumb" src="${escapeHtml(b.og_image)}" alt="" loading="lazy">`
      : `<div class="block-thumb" style="display:grid;place-items:center;font-family:var(--font-pixel);font-size:0.7rem;color:var(--ink-dim);">LINK</div>`;
  } else if (b.type === 'image') {
    const url = b.image_path
      ? sb.storage.from('garden-images').getPublicUrl(b.image_path).data.publicUrl
      : '';
    inner = `<img class="block-thumb" src="${escapeHtml(url)}" alt="${escapeHtml(b.title || '')}" loading="lazy">`;
  } else {
    inner = `<div class="block-note-preview">${escapeHtml(b.body || '')}</div>`;
  }
  let foot = b.title || (b.type === 'link' && b.url ? new URL(b.url).host : b.type);
  return `<article class="garden-block-card" data-type="${b.type}" data-id="${b.id}">
    <span class="badge">${b.type.toUpperCase()}</span>
    <div class="block-actions" data-admin>
      <button data-action="edit" data-id="${b.id}">EDIT</button>
      <button data-action="del" data-id="${b.id}">DEL</button>
    </div>
    ${inner}
    <div class="block-foot">${escapeHtml(foot)}</div>
  </article>`;
}

async function loadChannel() {
  const { data: ch } = await sb
    .from('garden_channels').select('*').eq('slug', slug).maybeSingle();

  if (!ch) {
    document.title = 'Not Found — Garden';
    titleEl.textContent = 'NOT FOUND';
    summaryEl.textContent = `No channel with slug "${slug}".`;
    setHTML(gridEl, '');
    gridEl.setAttribute('aria-busy', 'false');
    return;
  }

  currentChannel = ch;
  document.title = `${ch.title} — Garden — Jaegger Jose`;
  titleEl.textContent = ch.title.toUpperCase();
  summaryEl.textContent = ch.summary || '';
  nameEl.textContent = ch.title.toUpperCase();

  const { data: blocks } = await sb
    .from('garden_blocks').select('*')
    .eq('channel_id', ch.id)
    .order('position', { ascending: false })
    .order('created_at', { ascending: false });

  currentBlocks = blocks || [];
  if (currentBlocks.length === 0) {
    setHTML(gridEl, `<p class="garden-empty">empty channel.</p>`);
  } else {
    setHTML(gridEl, currentBlocks.map(renderBlockCard).join(''));
  }
  gridEl.setAttribute('aria-busy', 'false');

  // Tag cloud
  const freq = {};
  for (const b of currentBlocks) for (const t of (b.tags || [])) freq[t] = (freq[t] || 0) + 1;
  const list = Object.entries(freq).sort((a, b) => b[1] - a[1]);
  if (list.length > 0) {
    tagsBox.hidden = false;
    setHTML(tagCloud, list.map(([t, n]) =>
      `<span class="tag-chip">${escapeHtml(t)} <small>(${n})</small></span>`
    ).join(' '));
  } else {
    tagsBox.hidden = true;
  }
}

loadChannel();
```

- [ ] **Step 2: Commit**

```bash
git add js/garden-channel.js
git commit -m "feat(garden): channel page render"
```

---

### Task 3.4: js/garden-channel.js — Part 2（block detail modal）

**Files:**
- Modify: `js/garden-channel.js`（append）

- [ ] **Step 1: 加 openBlockModal + 點擊綁定**

```js
const blockModal     = document.getElementById('block-modal');
const blockModalBody = document.getElementById('block-modal-body');
blockModal.querySelector('.modal-close').addEventListener('click', () => blockModal.close());
blockModal.addEventListener('click', (e) => {
  if (e.target === blockModal) blockModal.close();
});

function openBlockModal(b) {
  let hero = '';
  let footRight = '';
  let host = '';
  if (b.type === 'link') {
    if (b.og_image) {
      hero = `<img class="modal-hero" src="${escapeHtml(b.og_image)}" alt="">`;
    }
    if (b.url) {
      try { host = new URL(b.url).host; } catch {}
      footRight = `<a class="modal-visit" href="${escapeHtml(b.url)}" target="_blank" rel="noopener noreferrer">VISIT ↗</a>`;
    }
  } else if (b.type === 'image') {
    const url = sb.storage.from('garden-images').getPublicUrl(b.image_path).data.publicUrl;
    hero = `<img class="modal-hero" src="${escapeHtml(url)}" alt="${escapeHtml(b.title || '')}">`;
  } else {
    const firstLine = (b.body || '').split('\n')[0];
    hero = `<div class="modal-quote">"${escapeHtml(firstLine)}"</div>`;
  }

  const tagsHtml = (b.tags && b.tags.length)
    ? `<div class="modal-tags">${b.tags.map(t => `<span>${escapeHtml(t)}</span>`).join('')}</div>`
    : '';
  const dateStr = new Date(b.created_at).toISOString().slice(0,10);

  setHTML(blockModalBody, `
    ${hero}
    <div class="modal-body">
      ${b.title ? `<h2 class="modal-title">${escapeHtml(b.title)}</h2>` : ''}
      ${b.body && b.type !== 'note' ? `<p class="modal-comment">${escapeHtml(b.body)}</p>` : ''}
      ${b.type === 'note' && b.body ? `<p class="modal-comment">${escapeHtml(b.body)}</p>` : ''}
      ${tagsHtml}
      <div class="modal-meta">
        <span>${dateStr}${host ? ' · via ' + escapeHtml(host) : ''}</span>
        ${footRight}
      </div>
    </div>
  `);
  blockModal.showModal();
}

gridEl.addEventListener('click', async (e) => {
  const actBtn = e.target.closest('.block-actions button');
  if (actBtn) {
    e.stopPropagation();
    return; // edit/del handled later
  }
  const card = e.target.closest('.garden-block-card');
  if (!card) return;
  const b = currentBlocks.find(x => x.id === card.dataset.id);
  if (b) openBlockModal(b);
});
```

- [ ] **Step 2: Commit + push**

```bash
git add js/garden-channel.js
git commit -m "feat(garden): block detail modal (link/image/note)"
git push origin main
```

---

### Task 3.5: 部署驗證 channel 顯示

- [ ] **Step 1: Playwright 訪問**

```
mcp__plugin_playwright_playwright__browser_navigate
  url: https://www.jaeggerjose.com/garden/typography/
mcp__plugin_playwright_playwright__browser_take_screenshot
  filename: garden-channel-empty.png
  fullPage: true
```

預期：看到 TYPOGRAPHY (上方 hero) + `empty channel.`。
若顯示 NOT FOUND：先檢查 vercel.json 部署是否生效；再確認 `Typography` channel 真的存在於 DB。

---

## Phase 4 — Admin block management

### Task 4.1: Block 表單 auth + type 切換

**Files:**
- Modify: `js/garden-channel.js`（append）

- [ ] **Step 1: Auth wiring + 表單**

```js
const authBar     = document.getElementById('auth-bar');
const authTrigger = document.getElementById('auth-trigger');
const loginModal  = document.getElementById('login-modal');
const loginForm   = document.getElementById('login-form');
const signoutBtn  = document.getElementById('signout-btn');
const authUser    = document.getElementById('auth-user');

authTrigger.addEventListener('click', () => {
  if (isAdmin) authBar.style.display = 'flex';
  else loginModal.showModal();
});
loginForm.addEventListener('submit', async (e) => {
  const fd = new FormData(loginForm);
  const { error } = await sb.auth.signInWithPassword({
    email: fd.get('email'), password: fd.get('password'),
  });
  if (error) { alert('Login failed: ' + error.message); e.preventDefault(); }
});
signoutBtn.addEventListener('click', async () => {
  await sb.auth.signOut();
  authBar.style.display = 'none';
});
sb.auth.onAuthStateChange((_evt, session) => {
  isAdmin = !!session;
  document.body.classList.toggle('is-admin', !!session);
  if (session) { authBar.style.display = 'flex'; authUser.textContent = session.user.email; }
  else authBar.style.display = 'none';
  loadChannel();
});

const blockFormModal = document.getElementById('block-form-modal');
const blockForm      = document.getElementById('block-form');
const newBlockBtn    = document.getElementById('new-block-btn');

function setBlockFormType(type) {
  blockForm.querySelectorAll('[data-type-field]').forEach(el => {
    el.style.display = (el.dataset.typeField === type) ? '' : 'none';
  });
}
blockForm.addEventListener('change', (e) => {
  if (e.target.name === 'type') setBlockFormType(e.target.value);
});

newBlockBtn.addEventListener('click', () => {
  blockForm.reset();
  setBlockFormType('link');
  blockForm.dataset.editId = '';
  document.getElementById('block-form-title').textContent = 'New Block';
  blockFormModal.showModal();
});
```

- [ ] **Step 2: Commit**

```bash
git add js/garden-channel.js
git commit -m "feat(garden): block form auth + type toggle"
```

---

### Task 4.2: og fetch 接通

**Files:**
- Modify: `js/garden-channel.js`（append）

- [ ] **Step 1: 加 fetch 按鈕 handler**

```js
document.getElementById('og-fetch-btn').addEventListener('click', async () => {
  const target = blockForm.url.value.trim();
  if (!/^https:\/\//i.test(target)) { alert('URL must start with https://'); return; }
  const btn = document.getElementById('og-fetch-btn');
  btn.disabled = true; btn.textContent = '...';
  try {
    const res = await fetch(`/api/og?url=${encodeURIComponent(target)}`);
    const j = await res.json();
    if (!j.ok) { alert('og failed: ' + j.error); return; }
    if (j.title && !blockForm.title.value) blockForm.title.value = j.title;
    if (j.description && !blockForm.body.value) blockForm.body.value = j.description;
    if (j.image) blockForm.og_image.value = j.image;
  } catch (err) {
    alert('og fetch error: ' + err.message);
  } finally {
    btn.disabled = false; btn.textContent = '→ Fetch';
  }
});
```

- [ ] **Step 2: Commit**

```bash
git add js/garden-channel.js
git commit -m "feat(garden): wire /api/og prefill"
```

---

### Task 4.3: 儲存 block（含圖片上傳）

**Files:**
- Modify: `js/garden-channel.js`（append）

- [ ] **Step 1: submit handler**

```js
blockForm.addEventListener('submit', async (e) => {
  if (e.submitter && e.submitter.value === 'cancel') return;
  if (!currentChannel) { e.preventDefault(); return; }

  const fd = new FormData(blockForm);
  const type = fd.get('type');
  const tags = String(fd.get('tags') || '').split(',').map(t => t.trim()).filter(Boolean);

  const payload = {
    channel_id: currentChannel.id,
    type,
    title:    fd.get('title') || null,
    body:     fd.get('body') || null,
    tags,
    position: Number(fd.get('position') || 0),
    url:      type === 'link'  ? (fd.get('url') || null) : null,
    og_image: type === 'link'  ? (fd.get('og_image') || null) : null,
  };

  if (type === 'image') {
    const file = fd.get('image_file');
    if (!file || file.size === 0) {
      if (!blockForm.dataset.editId) {
        alert('Please choose an image file');
        e.preventDefault();
        return;
      }
    } else {
      const safeName = file.name.replace(/[^a-zA-Z0-9._-]/g, '_');
      const path = `${currentChannel.slug}/${Date.now()}_${safeName}`;
      const { error: upErr } = await sb.storage.from('garden-images')
        .upload(path, file, { cacheControl: '31536000', upsert: false });
      if (upErr) { alert('upload failed: ' + upErr.message); e.preventDefault(); return; }
      payload.image_path = path;
    }
  }

  if (type === 'note' && !payload.body) {
    alert('Note requires body'); e.preventDefault(); return;
  }

  const editId = blockForm.dataset.editId;
  const result = editId
    ? await sb.from('garden_blocks').update(payload).eq('id', editId)
    : await sb.from('garden_blocks').insert(payload);
  if (result.error) { alert('save failed: ' + result.error.message); e.preventDefault(); return; }

  blockForm.reset();
  blockForm.dataset.editId = '';
  loadChannel();
});
```

- [ ] **Step 2: Commit**

```bash
git add js/garden-channel.js
git commit -m "feat(garden): save block with image upload"
```

---

### Task 4.4: Edit / Delete block

**Files:**
- Modify: `js/garden-channel.js`（修改既有 gridEl click handler）

- [ ] **Step 1: 取代既有 gridEl click handler（為了支援 edit/del）**

找到 Task 3.4 加的 `gridEl.addEventListener('click', ...)`，刪除整段，貼上：

```js
gridEl.addEventListener('click', async (e) => {
  const actBtn = e.target.closest('.block-actions button');
  if (actBtn) {
    e.stopPropagation();
    const id = actBtn.dataset.id;
    const action = actBtn.dataset.action;
    const b = currentBlocks.find(x => x.id === id);
    if (!b) return;
    if (action === 'del') {
      if (!confirm(`Delete this ${b.type}?`)) return;
      if (b.type === 'image' && b.image_path) {
        await sb.storage.from('garden-images').remove([b.image_path]);
      }
      const { error } = await sb.from('garden_blocks').delete().eq('id', id);
      if (error) { alert('delete failed: ' + error.message); return; }
      loadChannel();
    } else if (action === 'edit') {
      blockForm.reset();
      blockForm.dataset.editId = b.id;
      document.getElementById('block-form-title').textContent = 'Edit Block';
      setBlockFormType(b.type);
      blockForm.querySelector(`input[name="type"][value="${b.type}"]`).checked = true;
      blockForm.title.value    = b.title || '';
      blockForm.body.value     = b.body || '';
      blockForm.tags.value     = (b.tags || []).join(', ');
      blockForm.position.value = b.position || 0;
      if (b.type === 'link') {
        blockForm.url.value      = b.url || '';
        blockForm.og_image.value = b.og_image || '';
      }
      blockFormModal.showModal();
    }
    return;
  }
  const card = e.target.closest('.garden-block-card');
  if (!card) return;
  const b = currentBlocks.find(x => x.id === card.dataset.id);
  if (b) openBlockModal(b);
});
```

- [ ] **Step 2: Commit + push**

```bash
git add js/garden-channel.js
git commit -m "feat(garden): block edit/delete admin actions"
git push origin main
```

---

### Task 4.5: 完整 admin 流程驗證（Playwright）

- [ ] **Step 1: 等部署 ~30s**

- [ ] **Step 2: 訪問 + 登入**

```
mcp__plugin_playwright_playwright__browser_navigate
  url: https://www.jaeggerjose.com/garden/typography/
```

點右下小圓點登入。

- [ ] **Step 3: 建一個 link block（用 og fetch）**

點 `+ NEW BLOCK`：
- type: Link
- URL: `https://practicaltypography.com/`
- 按 `→ Fetch` → title / og_image 應自動填
- 加 tags `typography, web`
- Publish

- [ ] **Step 4: 建一個 note block**

- type: Note
- title: `negative space`
- body: `空白也是構圖`
- Publish

- [ ] **Step 5: 建一個 image block**

- type: Image
- 選一張本機 jpg/png
- title: 隨意
- Publish

- [ ] **Step 6: 截圖三型混合**

```
mcp__plugin_playwright_playwright__browser_take_screenshot
  filename: garden-3-block-types.png
  fullPage: true
```

預期：三張正方 card，各帶 LINK / NOTE / IMAGE badge。

- [ ] **Step 7: 點每張 card 開 modal，分別截圖**

```
browser_click ... (link card)
browser_take_screenshot filename=garden-modal-link.png
browser_press_key Escape
browser_click ... (note card)
browser_take_screenshot filename=garden-modal-note.png
browser_press_key Escape
browser_click ... (image card)
browser_take_screenshot filename=garden-modal-image.png
```

- [ ] **Step 8: 編輯 + 刪除**

Hover link card → 點 EDIT → 改 title → Publish → 看到更新。
Hover image card → 點 DEL → 確認 → 該 card 消失。

---

## Phase 5 — 跨頁整合

### Task 5.1: 三頁 nav 加 GARDEN

**Files:**
- Modify: `index.html`
- Modify: `journal.html`
- Modify: `works.html`

- [ ] **Step 1: index.html**

讀 index.html。在 nav (`<li><a href="works.html">PROJECTS</a></li>` 之後) 插入：

```html
        <li><a href="garden.html">GARDEN</a></li>
```

在 mobile menu (`<a href="works.html" class="mm-link">PROJECTS</a>` 之後) 插入：

```html
    <a href="garden.html" class="mm-link">GARDEN</a>
```

- [ ] **Step 2: journal.html**

對 journal.html 做同樣事（位置：PROJECTS 之後）。

- [ ] **Step 3: works.html**

對 works.html 做同樣事。

- [ ] **Step 4: Commit**

```bash
git add index.html journal.html works.html
git commit -m "feat(garden): add GARDEN to nav on all pages"
```

---

### Task 5.2: CSP img-src 放寬

**Files:**
- Modify: `vercel.json`

- [ ] **Step 1: 改 CSP-Report-Only**

讀 vercel.json，找到 `Content-Security-Policy-Report-Only` 的 value。
把 img-src 區段：

```
img-src 'self' data: blob: https://jnwnigwyyasdtsgliypa.supabase.co https://*.google-analytics.com https://*.googletagmanager.com;
```

改成：

```
img-src 'self' data: blob: https:;
```

- [ ] **Step 2: Commit**

```bash
git add vercel.json
git commit -m "chore(csp): widen img-src to https: for og thumbnails"
```

---

### Task 5.3: DESIGN_SPEC.md §14

**Files:**
- Modify: `DESIGN_SPEC.md`

- [ ] **Step 1: 在檔尾追加**

```markdown

## 14. Garden 系統

Garden 是 are.na 風格的 channel × block 收藏系統，跟 journal（LIFE）平行。

### 14.1 資料模型
- `garden_channels`：slug / title / summary / visibility(public|private) / position
- `garden_blocks`：channel_id / type(link|image|note) / title / body / url / image_path / og_image / tags[] / position

### 14.2 RLS
- anon 只看 `visibility='public'` 的 channel 與其 blocks
- authenticated（admin）全權

### 14.3 視覺
- Landing `/garden/`：像素索引列表，row hover 觸發 halftone overlay
- Channel `/garden/[slug]/`：正方 block grid（aspect-ratio 1/1），左上 badge 區分 type
  - link → `--moss`
  - image → `--akache`
  - note → `--ink`
- Modal 用原生 `<dialog>`：link 顯示 og_image + VISIT 鈕；image 顯示原圖；note 顯示 Shippori Mincho 引用版面

### 14.4 後端
- `/api/og?url=` 抓 og:image / title / description（SSRF 防護：https only、私網 IP 阻擋、5s timeout、1MB cap、redirect 重檢）
- Supabase Storage bucket `garden-images`，公開讀
```

- [ ] **Step 2: Commit + push**

```bash
git add DESIGN_SPEC.md
git commit -m "docs(garden): add §14 to DESIGN_SPEC"
git push origin main
```

---

## Phase 6 — 最終驗收

### Task 6.1: CSP / Lighthouse 檢查

- [ ] **Step 1: 確認 CSP 已部署**

```bash
sleep 30
curl -sI https://www.jaeggerjose.com/garden.html | grep -i 'content-security-policy'
```

預期：含 `img-src 'self' data: blob: https:`。

- [ ] **Step 2: Playwright 走全站，看 console 無 CSP violation**

```
mcp__plugin_playwright_playwright__browser_navigate url=https://www.jaeggerjose.com/garden/
mcp__plugin_playwright_playwright__browser_console_messages
```

預期：無 `[Report Only]` 訊息。

```
mcp__plugin_playwright_playwright__browser_navigate url=https://www.jaeggerjose.com/garden/typography/
mcp__plugin_playwright_playwright__browser_console_messages
```

預期：同上。

- [ ] **Step 3: Lighthouse**

Chrome DevTools → Lighthouse → Mobile + Performance + Accessibility 跑 `/garden/` 與 `/garden/typography/`。
記錄 LCP / CLS / INP / Accessibility 分數。LCP > 2.5s 或 CLS > 0.1 → 回頭查 block image 是否缺尺寸。

---

### Task 6.2: 私 channel RLS 二次驗證

- [ ] **Step 1: 登入 admin，建私 channel + 1 個 note block**

訪問 `/garden/`，新增 channel `secret-stash`、visibility=private、summary 隨意。
進入 `/garden/secret-stash/`，加一個 note `private probe`。

- [ ] **Step 2: 用 anon key 確認看不到私 channel**

```bash
ANON='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Impud25pZ3d5eWFzZHRzZ2xpeXBhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzY4MjkzOTMsImV4cCI6MjA5MjQwNTM5M30.62cJEKNqTt6P6bZMrVVcp_xLbD6tIuTjKgOI-SmSMAY'
curl -s "https://jnwnigwyyasdtsgliypa.supabase.co/rest/v1/garden_channels?select=slug,visibility&visibility=eq.private" \
  -H "apikey: $ANON" -H "Authorization: Bearer $ANON"
```

預期：`[]`。

- [ ] **Step 3: 登出後訪問 `/garden/secret-stash/`**

```
mcp__plugin_playwright_playwright__browser_navigate url=https://www.jaeggerjose.com/garden/secret-stash/
```

預期：顯示 `NOT FOUND No channel with slug "secret-stash".`。

- [ ] **Step 4: 重新登入 admin，確認私 channel 仍可見**

訪問 `/garden/`，admin 視角應看到 `SECRET-STASH` + PRIV badge。

---

### Task 6.3: 收尾

- [ ] **Step 1: 查 git log 順序**

```bash
git log --oneline -30
```

確認 commits 訊息清楚、按 phase 排列。

- [ ] **Step 2: 部署完成的最終冒煙**

```
mcp__plugin_playwright_playwright__browser_navigate url=https://www.jaeggerjose.com/
```

確認首頁 nav 有 GARDEN，點 GARDEN 進得去，看得到 channel。

- [ ] **Step 3: 釋出**

無需特別 release notes — git history 已足。可以開始用 GARDEN 收藏靈感了。

---

## 附錄 A：版本號 cache busting

每次改 `js/garden*.js` / `css/garden.css`，同步把 HTML 內 `?v=N` 數字 +1（與 DESIGN_SPEC.md §12 慣例一致）。

## 附錄 B：常見問題

| 症狀 | 原因 | 解法 |
|---|---|---|
| `/garden/typography/` 顯示 404 | Vercel rewrite 未生效 | 確認 vercel.json `rewrites` 區塊存在且部署 |
| 圖片不顯示 | bucket policy 缺 | 跑 Task 0.2 SQL |
| og fetch 慢 | 目標站慢 / cold start | 重試；或視為非關鍵自己填欄位 |
| Console CSP violation | img-src 未放寬 / 新外部腳本 | 看 violation directive 補白名單 |
| Login 失敗 | Supabase Auth 沒建 user | Supabase Dashboard → Authentication → Users 建一筆 |
| 私 channel 在 anon 視角還看得到 | RLS 未生效 | 跑 Task 0.1 SQL，確認 `enable row level security` 已執行 |

## 附錄 C：執行順序總覽

Phase 0 (3 tasks) → Phase 1 (5) → Phase 2 (4) → Phase 3 (5) → Phase 4 (5) → Phase 5 (3) → Phase 6 (3)
共 **28 個 task**，預估 **20 工時 / 2-3 天**。
